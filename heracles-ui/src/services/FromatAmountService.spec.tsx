import React from "react";
import FormatAmountService from './FormatAmountService'

describe("FormatAmountService", () => {
    beforeEach(() => {

    });

    test("return the correct formatted amount", () => {
        // expect(FormatAmountService.formatAmount({})).toBe(false)
    });
});
